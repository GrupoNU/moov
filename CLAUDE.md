# Moov Mail

> Webmail open source Gmail-class para Mailcow/Dovecot — primer producto open source de NU Desarrollos Conscientes
> Repo público: `github.com/GrupoNU/moov` · Licencia: AGPL-3.0

## Estado

**2026-08-08:** Proyecto iniciado 2026-08-07. Fase 0 (research) completa, ADR-001 aceptado. **Spikes S1 y S2 VALIDADOS.** S1: JMAP-sobre-Dovecot funciona contra nuestro Mailcow real, incluyendo Bulwark (cliente de terceros) en navegador — `docs/spikes/S1-jmap-sobre-dovecot.md` (H1-H7). S2: base técnica del sync engine validada — QRESYNC/CONDSTORE/IDLE/NOTIFY correctos en nuestro Dovecot 2.3.21.1 (NOTIFY colapsa el fan-out: 1 conexión observa N carpetas); go-imap/v2 requiere **rama v2 pinneada + patch set propio** (PR #757 QRESYNC validado end-to-end, fix encoder NOTIFY, exponer MODIFIED) — `docs/spikes/S2-go-imap-dovecot.md` (H1-H9 + arbitraje A4). Stack de referencia S1 corriendo en el VPS mail (VPN-only): Caddy (:8090) → Bulwark + jmap-proxy → Dovecot, con buzón de prueba `moov-test@atmosfera.cloud` (credenciales en `credentials/` de VPS_Mail, git-ignored). **S4 VALIDADO (2026-08-09):** corpus de MIME patológico en `testdata/mime-corpus/` (110 casos, manifest-como-spec) + harness dual-parser — cascada go-message → enmime → raw blob confirmada bidireccional, 0 panics/hangs — `docs/spikes/S4-corpus-mime.md` (H1-H9). **S3 VALIDADO (2026-08-09):** tsvector+GIN alcanza la vara Gmail-class a 5M mensajes (8/10 shapes, margen 4x-30x, 607 qps concurrente) con 3 configs obligatorias (`gin(account_id,tsv)` compuesto, `force_custom_plan`, `STATISTICS 4000`); ranking/count exacto se resuelven por producto (count capeado, relevancia opt-in acotada, pool separado); **Meilisearch fuera del MVP** — `docs/spikes/S3-benchmark-fts.md` (H1-H11). **FASE DE SPIKES COMPLETA (4/4).** Spec L2 del sync engine redactada (`docs/specs/L2-sync-engine.md`, 2026-08-09): 8 épicas E1-E8, contracts, y arbitrajes A5 (estado volátil en tabla lateral), A6 (labels híbrido keywords+METADATA) y A7 (initial sync doble camino) — **ACEPTADA** (arbitrajes delegados por Diego al director técnico, 2026-08-09). **E1-E4 COMPLETAS** (2026-08-10): E1 scaffolding+CI; E2 `internal/imap` (vendor go-imap rama v2 + patch set 0001-0003 validado contra Dovecot real; V1 ejecutada: METADATA ok, **techo durable de keywords Maildir = 26** — corrección al A6); E3 `internal/store`+`internal/blob` (schema A5 con `message_state`, repertorio de búsqueda S3 como métodos, fix de unaccent en query-side); E4 `internal/parser` (cascada con 7 mitigaciones, corpus 110/110, 3 bugs cazados por fuzzing incl. DoS superlineal en enmime). **E5+E6+E7 COMPLETAS (2026-08-11): el sync engine funciona de punta a punta** — initial sync (10k en ~50 s, crash-recovery probado), incremental QRESYNC + watcher NOTIFY (mail nuevo → store en ~500 ms mediana contra Dovecot real), reconciler, y aprovisionamiento con crypto (ciclo real validado contra Mailcow). Falta E8 (observabilidad, chica). Siguiente bloque: **L2 del servidor JMAP ACEPTADA** (`docs/specs/L2-jmap-server.md`, 2026-08-11): 4 épicas J1-J4, hito de cierre = Bulwark leyendo mail vía nuestro server. J1 (core+HTTP+auth) en curso.

**2026-08-12 — HITO J4 ALCANZADO: Bulwark lee mail real a través de NUESTRO servidor.** Stack propio desplegado en el VPS (`/opt/moov/`, **corriendo y sano**, VPN-only): moovd (distroless non-root, 42 MB, migraciones embebidas al arranque) + PostgreSQL 17, unido a `mailcowdockerized_mailcow-network` + red interna propia para PG; **nada publicado**. Cuenta `moov-test@atmosfera.cloud` aprovisionada vía moovctl (credencial validada por IMAP LOGIN real contra Dovecot, cifrada AES-256-GCM, password del usuario descartada); 11 buzones descubiertos, 4 mensajes sincronizados, 0 parse-failures, watcher NOTIFY conectado. Caddy de S1 rutea a moovd (**rollback a jmap-proxy = 1 línea**, documentado en `deploy/Caddyfile.pilot`; el proxy sigue vivo como oráculo). **Verificado en navegador real (Playwright, no curl):** login por NUESTRA auth, sidebar con las 12 carpetas y contadores exactos (2 sin leer / 4), inbox renderizando los 4 mails con asunto/remitente/preview, **búsqueda FTS** ("1 resultado encontrado" para "arquitectura"), navegación de carpetas. **8.903 respuestas 200, cero 4xx/5xx no-esperados.** Pendiente el clic visual de Diego sobre un mail: el panel de lectura no se activa con eventos sintéticos de Playwright (limitación del harness); el `Email/get` con bodyValues + `Thread/get` encadenado está verificado server-side con la petición exacta de Bulwark. **Dos gaps del risk map cerrados por tráfico real de Bulwark:** (1) sort **`hasKeyword`+`receivedAt`** — Bulwark abre TODA carpeta con ese par y sin él el inbox salía **vacío**; era un gap de conformidad real (§4.4.2 lo lista como SHOULD); (2) **`filter:null`** (enumeración de cuenta, `store.ListAccountMessages`). **JMAP TestSuite oficial: INUTILIZABLE, con evidencia** — (a) sin licencia (`license: null`, sin LICENSE, sin campo en package.json → all rights reserved, incompatible con un repo público AGPL), (b) arquitectónicamente write-only: su `cleanAccount()` **borra y resiembra la cuenta en el *setup***, antes del primer test, y falla en `Email/set`; `--filter` no puede saltearlo. En su lugar corre en CI (job `conformance`, PG service container) una **suite de conformidad propia citada cláusula por cláusula al RFC 8620/8621**: 12 tests verdes + 9 skips explícitos con razón (L2 §2.5: "nunca silencioso"). El único aporte real del suite oficial fue delatar el `filter:null`, que se implementó. **E8-lite:** métricas Prometheus **sin dependencias nuevas** (exporter propio: el vendor queda hermético) — sync lag por cuenta, breaker, histogramas HTTP/método, parse-failures, build info — más `/healthz`, en listener aparte nunca proxeado. Suite completa race-clean (15 paquetes), lint 0 issues, gofmt limpio.

**2026-08-20 — FASE 2 COMPLETA (W1-W4): Bulwark OPERA mail real a través de nuestro servidor.** El hito de cierre de `docs/specs/L2-jmap-write.md` está alcanzado: la fase 1 probó que leemos bien, la 2 prueba que escribimos **sin romper ni duplicar nada**. Piloto redeployado (commit `w4b`, **corriendo y sano**) con W1 (writes), W2 (`Mailbox/set`), threading real, W4a (SSE) y W3 (envío con undo). **Migraciones 0004+0005 aplicadas sobre el corpus real: 29,77 s** (0004 backfilleó `thread_id` sobre 26.869 mensajes; 0005 ~0,2 s) — **no es sub-segundo sobre un store poblado**, y el daemon no sirve hasta terminar: documentado en `deploy/README.md`. Threading verificado sobre datos reales: **1.168 hilos multi-mensaje** en la cuenta 2, el mayor con 24 mails encadenados por References/In-Reply-To. Cuenta 2 (`diego@gruponu.com`) **intacta: 26.869 mensajes, 24 buzones** — todas las escrituras se confinaron a `moov-test`. Primera validación en vivo del **fix de red compose-owned**: `moov-spike` sobrevivió a la recreación del contenedor sin `docker network connect` manual (bajo J4 eso era un 502 garantizado).

**SSE medido (el AC que W4a no pudo cerrar):** flag por IMAP crudo → `StateChange` en un EventSource vivo **a través de Caddy**, 3 corridas: **721 / 942 / 671 ms**, todas <1 s, y el state string pusheado **idéntico** al que devuelve `Email/get` (es señal de invalidación, no una pista). **Caddy no bufferea** — el evento llega en un stream que nunca termina; `X-Accel-Buffering: no` va server-side y `reverse_proxy` de Caddy stremea por defecto (a diferencia de nginx): **no hizo falta tocar `flush_interval`**.

**Ciclo completo verificado, con el mail entrando solo en el navegador:** draft (`Email/set create` → MIME ensamblado + APPEND `\Draft`, 192 ms) → `EmailSubmission/set` → ventana de undo → SMTP → **copia en Sent EXACTAMENTE UNA** (Message-ID coincidente, el peor fallo posible evitado) → **copia entregada llegando a INBOX y apareciendo sola en Bulwark vía SSE, sin refresh manual** (sidebar pasó a "Bandeja 4/6, Enviados 1/1, Archivo 1" en vivo; el asunto renderizado y verificado en el DOM). **Undo probado de verdad:** submission cancelada dentro de la ventana quedó `canceled`, y Sent vacío + INBOX sin rastro **prueban que nunca se transmitió**. Escrituras medidas: flags **19-44 ms**, archivar **166 ms**, crear/renombrar buzón **57-180 ms** — dentro de la vara de regla 1. **Un outlier honesto: borrar buzón tarda 1,8-6,1 s** (el DELETE de IMAP exige soltar el buzón seleccionado y compite con el reconciler del watcher); es correcto pero **no cumple la vara** — queda anotado, no parcheado sobre la bocina. **199 respuestas 200, cero 4xx/5xx no-esperados, cero ERROR y cero WARN en el log del server.** Un único gap de shape lo delató el propio servidor con un error preciso (`identityId` es `"primary"`, no `"i1"`) — **no hubo ningún `unknownMethod` que cerrar**: Bulwark no llamó `EmailSubmission/query`. Sigue pendiente el clic visual de Diego (misma limitación exacta de J4: el panel de lectura no se activa ni con eventos sintéticos ni con click real por coordenadas; `Email/get`+`Thread/get` encadenados verificados server-side).

**Métricas del outbox (E8-lite, cierre del pendiente de W3):** `moov_submissions_total{result}` — `sent` (250 leído **y persistido**), `failed` (5xx permanente o tope de reintentos), `canceled` (undo); un requeue transitorio **no cuenta** (el mail todavía puede salir). Sin dependencias nuevas ni reestructuración: dos seams tipo `Notifier` (`submit.Observer` y `mail.SubmissionObserver`) para que ni el executor ni la capa JMAP importen el exporter, cosidos en `cmd/moovd` y **pinneados por test** porque el acuerdo entre las tres constantes no lo verifica el compilador. Los tres contadores **verificados en producción** (`sent=1`, `canceled=1`). **Bug de observabilidad encontrado y documentado, NO parcheado:** `moov_sync_lag_seconds` lee `sync_log.last_success_at`, que sólo escribe el sync inicial — el watcher en régimen avanza `mailboxes.last_synced_at`, así que el piloto reporta **8,6 días de lag mientras entrega mail en segundos**. No es pérdida de datos, es la métrica mirando la tabla equivocada; **no alertar sobre ese gauge** hasta arreglarlo (query honesta en `deploy/README.md`). Suite completa race-clean (15 paquetes + tools), lint v2.1.6 **0 issues**, build/vet/gofmt limpios.

**2026-08-30 — RE-PLANIFICACIÓN GMAIL-FIRST: plan v2 INVALIDADO, canon + plan v3 listos para firma.** El plan L3 v2 tomó a Bulwark como piso y se rompió en el primer contraste real (favicons); mandato de Diego: Gmail define QUÉ y POR QUÉ, Bulwark solo aporta CÓMO. Producido con 4 agentes de research en paralelo (2×Fable, 2×Opus), auditados por el director con spot-checks contra fuente viva y código: **`docs/research/06-gmail-canon.md`** (canon Gmail verificado contra documentación de Google recuperada en vivo — filtro de 3 preguntas, canon de interacción CORE/SEC, schema de ajustes DIRECT/ADAPT/N-A, registro de omisiones deliberadas con sus razones, kill-list de Bulwark con razón citable, registro UNSOURCED, 10 correcciones al doc 05) y **`docs/specs/L3-gmail-class-plan.md` v3** (11 épicas E0-E11 con tallas, arbitrajes GC-1…GC-10, 8 decisiones D-1…D-8 para firma de Diego, diferidos nombrados, 7-9 semanas incluyendo gates). **Verificación adversarial ejecutada e incorporada** (16 hallazgos; los 7 obligatorios corregidos — el más grave: snooze/mute violaba "Dovecot es la fuente de verdad" → diseño GC-10). Hallazgos clave que reordenan todo: el rechazo de `collapseThreads` (`query.go:146-149`) está **obsoleto** desde la migración 0004 (conversación desbloqueada server-side, verificado por el director); snooze/mute/schedule-send son CORE en Gmail (la v2 los difería); la paridad de notificaciones es foreground (Gmail web solo notifica con pestaña abierta — Web Push NO es brecha); solo 4 features de Gmail son AI-gated (paridad = 100% determinística); el proxy de imágenes que YA tenemos es la precondición del display-por-defecto de Gmail. **Pendiente: firma de Diego (plan §9 + decisiones D-1…D-8). Sin código, sin deploy, sin push.**

**2026-08-31 — PROGRAMA GMAIL-CLASS EJECUTADO Y DESPLEGADO.** Diego firmó el plan v3 completo (D-1…D-8 con las recomendaciones; deploy autorizado en la firma). Ejecución con equipos de agentes (Fable en E6/E10/gate, Opus en el resto; director Fable audita todo): **las 11 épicas E0-E11 construidas** — preferencias server-side v1+v2 con consumidores cableados, conversación punta a punta (collapse 6-8 ms, lector expandible con recorte de citas seguro), triage completo (spam/undo-z/hover/chords/unsubscribe RFC-parsing/papelera), búsqueda Gmail (operadores, OR acotado, SearchSnippet anti-XSS, **techo D-7 resuelto: 100.000 medido**), snooze/mute/schedule con GC-10 (snooze = MOVE+re-APPEND IMAP real; tabla `threads` 0009; `Thread/changes` implementado), ajustes completos con búsqueda (D-5), Sieve completo (ManageSieve C0, particionado por origen con backup-antes-de-takeover, vacation `:days 4` con todas las guardas verificado contra Pigeonhole real, never-spam probado empíricamente, reenvío con verificación por token, Quota vía IMAP), labels bajo el techo 26 con escape RFC 6901, PWA instalable + **offline real** (IndexedDB v2, Outbox, búsqueda offline) + notificaciones 2-modos, confianza E10 (25 intentos de bypass de imágenes: cero; invariantes MDN/URL pinneadas en ambas capas; extensiones bloqueadas declared==enforced cross-language), teclado Gmail completo por `event.code`, TNEF (D-6) con decoder propio acotado (1,79M fuzz execs limpias — y cazó un bug real de bodypart que dropeaba hijos en silencio). **Gate final (auditor Fable independiente): DEPLOYABLE-WITH-FINDINGS — los 3 obligatorios corregidos antes del deploy** (`event.code` inalcanzable en producción, prefs-v2 sin consumidores, TNEF faltante); informe en `docs/reports/gate-final-gmail-class.md` con el guion de prueba del dueño (15 pasos). Suites: **web 2099 tests / Go race-clean 18 paquetes / lint 0** en el árbol desplegado. **Deploy ejecutado**: migraciones 0007-0010 en 2,8 s sobre el corpus real, 4 watchers NOTIFY conectados, PWA nueva sirviendo en los 4 dominios (SW registrado, manifest OK, cero errores de consola), Bulwark intacto como oráculo. Un drift server-side encontrado y commiteado al repo (mount del dist en caddy-public). Backup en `/opt/moov/src.pre-gmail` (rollback = swap + compose up). **Harness de sesión: sin Go local, todo Go corre en el VPS (ver memoria del proyecto). Push al repo público autorizado y ejecutado (2026-08-31, con pasada de documentación open-source: README real pre-1.0, docs/README índice, CONTRIBUTING/SECURITY al día). Diferidos vigentes en plan §6** (escotilla-2 de mute, splits GC-3, RFC 8058 server, e2e en repo, superstars, Manage subscriptions, operadores de cola).

**2026-08-31 (2) — E12 PARIDAD DE MEMORIA MUSCULAR: construida y desplegada.** Mandato de Diego al probar el piloto: Gmail-class incluye el DÓNDE — "la gente sabe usar Gmail con los ojos cerrados; que nadie necesite un manual". Método corregido: canon de layout **verificado contra el Gmail vivo** (sesión del dueño, autorizada; 12 capturas locales jamás commiteadas) → `docs/research/07-gmail-layout-canon.md` → implementación en 7 bloques con commit por bloque: **B1** barra superior Gmail (ruedita ARRIBA a la derecha, búsqueda centrada con sliders, hamburguesa colapsa el riel) · **B2** ajustes rápidos como panel acoplado desde la derecha con vistas previas SVG (densidad/tema/bandeja/panel de lectura sobre las prefs existentes) · **B3** Configuración como PÁGINA ruteada con pestañas horizontales y filas de dos columnas (el popup murió; búsqueda D-5 en el header) · **B4** chrome de lista (estrella clickeable, paginación "1–50 de N" con flechas sobre el paging real de D-7, dropdown de selección, clip de adjuntos, fecha forma-Gmail) · **B5** divisor arrastrable lista/lector (mouse+touch+teclado, role=separator, persistido) · **B6** compositor como tarjeta flotante abajo-derecha (Enviar azul abajo-izquierda, minimizar/maximizar) · **B7** panel de búsqueda avanzada colgando de la caja con "Crear filtro" cableado al builder de E6. **2221 tests / lint 0 / build limpio.** Deploy sin downtime (E12 es solo web: sync + force-recreate de caddy-public por el inode del bind-mount — la trampa documentada del deploy anterior; hash del bundle verificado servido, cero errores de consola). Las 4 consultas de Diego quedaron resueltas (ancho ajustable, estrella en fila, paginación; categorías → fase IA). **Pendiente: gate visual lado-a-lado autenticado (necesita MOOV_TEST_USER/PASSWORD o los ojos de Diego) y push de E12.**

**2026-09-08/09 — REVISIÓN LADO A LADO + TANDA 1.** Método nuevo (pedido de Diego): Gmail y su cuenta real de Moov abiertas en el navegador de automatización; conductor único en solo-lectura capturó 22 pares a pantalla completa, 6 analistas por sección, el director auditó y **diagnosticó en el DOM vivo** — informe `docs/reports/revision-visual-2026-09-08.md` (7 P0, 4 de una línea; P0-1 del panel de lectura = `grid-area: divider` huérfana en el layout derecha creando pistas implícitas — invisible para jsdom). **T1 ejecutada y desplegada** (7 commits web + prefs v3 `folderVisibility`): split de lectura con placement explícito, menús opacos, búsqueda que no navega al tipear, Escape suelta el foco, riel curado (orden Gmail, "Más" plegable, carpetas de sincronización de Outlook ocultas por política con tabla Carpetas en Config→Etiquetas, "Archivo" desambiguado, reset del chrome de botón en filas virtuales), lector con fila de 8 iconos + ⋮, filas muertas de Recibidos fuera. 2294 tests. **Bug de infraestructura cazado al verificar en vivo: sin `Cache-Control`, el navegador retenía el `index.html` viejo (frescura heurística) y el SW servía los assets viejos cache-first — los deploys no llegaban a los usuarios.** Fix: Caddy `no-cache` para shell/sw/manifest + `immutable` para `/assets/*`, y el SW navega con `cache: "no-cache"` (`25ab236`); verificado en vivo. Tests de invariante CSS agregados porque 3 P0 solo eran visibles en navegador real. Quedan T2-T5 del informe y 5 decisiones de criterio (§4) para Diego. Cuentas reales: cero escrituras (1 no-leído abierto por accidente, declarado). El admin API de Caddy está deshabilitado: los cambios de Caddyfile se aplican con `--force-recreate caddy-public`, no con `caddy reload`.

**2026-09-09 (2) — TANDAS 2-5 COMPLETAS Y DESPLEGADAS: el informe de revisión queda cerrado.** Diego firmó las 5 decisiones de criterio ("sí a tus recomendaciones": caret de programar adosado a Enviar; búsqueda en toda la cuenta; sin punto azul de no-leído; **iframe se mantiene**, altura por estimación; IA para Importantes/categorías). Cuatro agentes web en paralelo con commit-por-ítem y staging por hunks en archivos compartidos (`MailScreen.tsx`, `strings.ts`): **T2** lista (hover con elevación y fecha oculta, leídos tintados, cota "de más de N", ⋮ de vista, barra de verbos solo con selección, `?p=N` en la URL, margen j/k, snippet sin URLs, densidad 32/40/48, contador de hilo) · **T5** ajustes (miniaturas de tema, panel 22rem, previews en Recibidos con `OptionGroup` compartido, radios en vez de selects, densidad y ancho máximo, import/export JSON de filtros, orden ↑↓ visible con ≥2, estado vacío de etiquetas con ejemplos, paleta 24 AA, "Guardado ✓") · **T3 (Fable)** lector (altura del iframe estimada en el padre desde el HTML saneado — sandbox byte-idéntico con test que falla ante cualquier `allow-*`; conversación abre solo el último; doble chevron + N de M; flecha+⋮ por mensaje; píldoras al pie; chips de carpeta/etiqueta; adjuntos como tarjetas con miniatura; **imágenes `cid:` ahora se muestran** vía `data:` ya permitido por la CSP sin meter tokens en el srcdoc; para mí ▾) · **T4** compositor y búsqueda (barra de formato al pie tras `Aa`, papelera derecha, fila de 9 controles, Cc/Cco muted, Enviar píldora con caret de programar que se retira offline, "De" colapsado, **firma nueva se sembraba solo en el wire — estaba rota, arreglada**; sugerencias por valores de operador, 5 mensajes en el dropdown, chips dentro del popup, strip de contexto, ancla de fecha, nota de "No incluye", ayuda a 2 columnas; el alcance de búsqueda YA era account-wide — el defecto era la etiqueta). **Suite: 2294 → 2562 tests.** Deploys web sin downtime con hash verificado; gate en vivo en la sesión del dueño (grilla 4 pistas, sandbox intacto, frame corto 112px, riel 6 filas). Higiene: `.playwright-mcp/` (capturas con correo real) se borró de todas las copias del árbol en el VPS — **excluirlo del tar de deploy**. Lección de gate: `npm run typecheck` (`tsc -b`) es el gate, no `tsc --noEmit` por archivo. Queda: verificación de escritura (archivar/enviar/filtros) con `moov-test`, y las decisiones sobre "Importantes"/categorías en fase IA. **T6 (mismo día, 6 commits hasta `fd73561`, suite 2589, desplegada):** búsqueda centrada-izquierda `clamp(420px,40vw,720px)`; punto azul de no-leído eliminado (§4.3); Redactar 48×135 con acento claro (`color-mix` 18%) — el riel gasta el acento sólido en la fila activa; contadores del riel como texto gris tabular; fila activa como píldora rellena opaca (16%) y sin `:focus` bare; contexto de vista (`is:starred`/`in:snoozed`/`label:`) como chip en la caja con ✕. **El informe de revisión 2026-09-08 queda cerrado**; próximo programa: **marca por dominio** (kickoff entregado a Diego: paso 1 branding de punta a punta — barra, paleta AA derivada del primary, manifest/iconos por host, docs del operador; paso 2 admin UI solo diseño, requiere decisión de roles).

## Reglas rectoras del proyecto (fijadas por Diego — no negociables)

1. **Vara Gmail-class en diseño Y performance.** El benchmark es Gmail/Fastmail/Superhuman, nunca otros webmails open source. Los criterios son medibles (ADR-001 §6): búsqueda y acciones <100 ms percibidos, push real, teclado completo, undo send, PWA offline.
2. **Arbitrajes: siempre lo más potente del mercado actual, con los estándares más altos y las mejores prácticas.** La opción conservadora solo se acepta como fase intermedia explícita, nunca como destino.
3. **Es la cara open source de NU** — calidad de código, docs, tests y gobernanza son parte del producto.

## Jerarquía de trabajo (modelo de sesión)

- **Director/auditor: el modelo principal de la sesión** (preferencia de Diego: Fable 5, `/model claude-fable-5[1m]`). Dirige, arbitra entre agentes, audita todo lo que producen, sintetiza y firma decisiones. No delega la auditoría.
- **Subagentes: modelos más económicos según complejidad de la tarea** — `opus` (Opus 5) por defecto para research/implementación estándar; `sonnet`/`haiku` solo para tareas triviales/mecánicas. Nunca comprometer calidad en tareas críticas por ahorrar.
- Ante recomendaciones en conflicto entre agentes, el director arbitra con la regla 2 (lo más potente del mercado, estándares más altos) y documenta el arbitraje (como los A1-A3 de `docs/research/00-sintesis-fase0.md`).
- Trabajo paralelo: lanzar agentes independientes en simultáneo (como los 4 de la Fase 0); el director consolida al final.

## Arquitectura (resumen — el detalle manda en `docs/adr/ADR-001-arquitectura.md`)

- **Opción B:** sync engine (Go) que sincroniza Dovecot vía IMAP (CONDSTORE/QRESYNC/IDLE) a store propio (PostgreSQL 17 + blobs content-addressed sha256 + índice FTS) y expone **JMAP estándar** (RFC 8620/8621, subset por fases) a una **PWA React/TypeScript**. Push por SSE.
- **Mailcow no se toca jamás.** Stack Docker propio unido a `mailcowdockerized_mailcow-network`. Todo por IMAP/SMTP/Sieve/API. **NUNCA montar ni tocar el filesystem de vmail** (corrupción garantizada con `maildir_very_dirty_syncs`).
- **Dovecot es la fuente de verdad**; Moov es cache reconstruible.
- Auth: IMAP LOGIN de validación → app password vía API Mailcow (scope imap+smtp+sieve), cifrada AES-256-GCM, password del usuario descartada.
- Seguridad HTML: 3 capas (bluemonday + DOMPurify + iframe sandbox sin allow-scripts + CSP). Proxy de imágenes con HMAC + anti-SSRF.
- `go-imap/v2` SIEMPRE vendorizado y encapsulado tras `internal/imap` — nunca usar `imapclient` fuera de ese paquete.
- JMAP TestSuite en CI desde el día 1. `jmap-perl` (github.com/jmapio/jmap-perl) es el plano de referencia del mapeo IMAP↔JMAP.

## Convenciones

- Código, comentarios, commits e issues públicos: **inglés**. Comunicación con Diego: español.
- Commits: `tipo(scope): descripción` (feat/fix/docs/refactor/chore/test).
- Testing obligatorio (política de ingeniería del grupo): bug fix = test primero; feature = tests por AC.
- NUNCA push sin confirmación de Diego.
- El corpus de MIME patológico (`docs/research/04` §4.2) debe existir ANTES que el parser.

## Entorno de validación

- Mailcow real de referencia: VPS mail de Grupo NU (repo `D:\git\VPS_Mail`, IP-A 217.216.83.79, Tailscale `100.123.119.124`). `SKIP_FTS=n` (Flatcurve activo). Red: `mailcowdockerized_mailcow-network`.
- Cuentas de prueba para spikes: crear buzones dedicados (NUNCA usar buzones productivos de clientes para pruebas de escritura).
- Caso de estrés de referencia: buzones Crash (89 cuentas, 584 GB backup-only en Mailcow).

## Spikes pendientes (bloqueantes antes de codear el producto)

| # | Spike | Valida |
|---|---|---|
| S1 ✅ | jmap-perl + Bulwark contra nuestro Mailcow | JMAP-sobre-Dovecot funciona, cliente de terceros incluido — **VALIDADO 100% 2026-08-08** |
| S2 ✅ | go-imap/v2 (rama v2 pinneada) contra nuestro Dovecot: QRESYNC, CONDSTORE, IDLE, NOTIFY multi-mailbox | Base técnica del sync engine; fan-out colapsado confirmado (1 conexión, N carpetas) — **VALIDADO 2026-08-08** |
| S3 ✅ | Benchmark tsvector+GIN, 5M mensajes en el VPS real | 8/10 shapes con margen 4x-30x; 3 configs obligatorias (gin compuesto, force_custom_plan, STATISTICS 4000); Meilisearch fuera del MVP — **VALIDADO 2026-08-09** |
| S4 ✅ | Corpus de MIME patológico (110 casos) + harness dual-parser | Cascada go-message → enmime → raw blob validada; 0 panics/hangs — **VALIDADO 2026-08-09** |

## Para retomar rápido

1. `docs/adr/ADR-001-arquitectura.md` — la decisión completa (2 páginas)
2. `docs/research/00-sintesis-fase0.md` — síntesis auditada + arbitrajes + riesgos
3. Los informes 01-04 de `docs/research/` solo si necesitás el detalle de un área
